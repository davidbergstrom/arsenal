<PreferenceScreen xmlns:android="http://schemas.android.com/apk/res/android">

    <ListPreference
            android:key="driver_food"
            android:title="@string/pref_title_food"
            android:defaultValue="-1"
            android:entries="@array/pref_food_titles"
            android:entryValues="@array/pref_food_values"
            android:negativeButtonText="@null"
            android:positiveButtonText="@null" />

</PreferenceScreen>
